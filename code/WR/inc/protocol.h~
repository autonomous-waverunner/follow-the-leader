#ifndef PROTOCOL_H
#define PROTOCOL_H

#include "path_alg.h"

#define C_COORDINATES 'C'
#define C_WAIT 'W'
#define C_PARAMS 'P'

struct msg_coordinates {
  double utc, latitude, longitude;
  char crc;
};

struct msg_parameters {
  pid_v thottle, nozzle; // 7 doubles  each
  double one, two, three, four;
  char crc;
};

#endif //PROTOCOL_H
